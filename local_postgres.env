# Local PostgreSQL Configuration
# Copy this to .env file and modify values as needed

# Django Settings
SECRET_KEY=django-insecure-w^=fb$xyf-ku5@n^*ek4#x(-iijee+kj$ixqxmw3wr0zjz@y(*
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Local PostgreSQL Database Settings
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/fraud_detection_local

# Alternative: Individual PostgreSQL settings
POSTGRES_DB=fraud_detection_local
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Instructions:
# 1. Install PostgreSQL from: https://www.postgresql.org/download/windows/
# 2. During installation, set password for 'postgres' user
# 3. Replace 'your_password' with the password you set
# 4. Copy this file to .env
# 5. Restart your Django server
