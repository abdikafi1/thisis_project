# Local PostgreSQL Configuration
# Copy this to .env file and modify values as needed

# Django Settings
SECRET_KEY=django-insecure-w^=fb$xyf-ku5@n^*ek4#x(-iijee+kj$ixqxmw3wr0zjz@y(*
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Local PostgreSQL Database Settings
# Use this Neon PostgreSQL database URL for production
DATABASE_URL='postgresql://neondb_owner:npg_LXrJzj85qwcC@ep-soft-truth-a14lkz4p-pooler.ap-southeast-1.aws.neon.tech:5432/neondb?sslmode=require'

# Alternative: Individual PostgreSQL settings (for local development)
POSTGRES_DB=fraud_detection_local
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Alternative: Individual PostgreSQL settings
POSTGRES_DB=fraud_detection_local
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Instructions:
# 1. Copy this file to .env in the root directory
# 2. For production, use the Neon database URL above
# 3. For local development, use the individual PostgreSQL settings below
# 4. Install PostgreSQL from: https://www.postgresql.org/download/windows/
# 5. During installation, set password for 'postgres' user
# 6. Replace 'your_password' with the password you set
# 7. Restart your Django server
